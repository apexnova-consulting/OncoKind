# App (for Stripe redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Stripe (set all live price IDs from Dashboard → Products)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ID=price_...                    # Legacy Pro monthly (fallback if PRO_MONTHLY not set)
STRIPE_PRICE_ID_PRO_MONTHLY=price_...         # Legacy Pro — monthly
STRIPE_PRICE_ID_PRO_YEARLY=price_...          # Legacy Pro — yearly
STRIPE_PRICE_ID_ADVOCATE_MONTHLY=price_...    # Advocate Plan — monthly
STRIPE_PRICE_ID_ADVOCATE_YEARLY=price_...     # Advocate Plan — yearly
STRIPE_PRICE_ID_ENTERPRISE_UNLIMITED=price_... # Professional — monthly unlimited users
STRIPE_PRICE_ID_ENTERPRISE_PER_SEAT=price_...  # Professional — monthly per seat

# Anthropic (Clarity engine)
ANTHROPIC_API_KEY=sk-ant-...

# Required for patient_reports encryption (32 bytes base64)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=

# Advocacy & Financial Navigation
FINANCIAL_AID_CRON_SECRET=replace-me-with-a-long-random-string
FINANCIAL_AID_SCRAPER_MODE=live
# Optional local/dev override if you want Playwright to use a local Chrome/Chromium binary.
PLAYWRIGHT_EXECUTABLE_PATH=
# Optional custom UA for external funding sites.
FINANCIAL_AID_SCRAPER_USER_AGENT=

# Admin / check-in reminders
ADMIN_EMAILS=founder@example.com
CHECK_IN_CRON_SECRET=replace-me-with-a-long-random-string
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=OncoKind <hello@oncokind.com>

# E2E / Playwright
ENABLE_E2E_TEST_ROUTES=0
BASE_URL=http://localhost:3000
TEST_FREE_EMAIL=
TEST_FREE_PASS=
TEST_ADVOCATE_EMAIL=
TEST_ADVOCATE_PASS=
